I have incorporated comments in the code for each function and how it works.

I chose java to implement this as java is object oriented and supports concepts like polymorphism, abstract that I have used in the design.
Image will the parent class that contains the operations and values that are common to both Color image and grey image.
ColorImage and GreyImage classes extend the Image class to define behaviors that are specific to them. 